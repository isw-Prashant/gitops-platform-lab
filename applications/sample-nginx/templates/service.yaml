apiVersion: v1
kind: Service

metadata:
  name: nginx

spec:
  selector:
    app: nginx

  ports:
    - port: {{ .Values.service.port }}

      targetPort: {{ .Values.service.port }}

  type: {{ .Values.service.type }}
